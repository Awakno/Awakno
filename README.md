```py 
class Me:
    def __init__(self):
        super().__init__()
        self.name = "Awakno"
        self.age = 404
        self.discord = "awakno"
    def skill(language):
        if isinstance(language,python):
            skills = {
                "Discord API": true,
                "Request": True,
                "Backend": True,
                "API":True,
                "Automatisation": True,
                "Front-End": "Learning"
            }
        if isinstance(language,javascript):
            skills = {
                "FrontEnd": True,
                "BackEnd": False
             }

        
```
